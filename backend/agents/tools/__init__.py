"""ADK function tools — Sentinel's capabilities exposed to the LLM agent runtime.

Each tool wraps a step module + the configured connectors, so the Google ADK
``LlmAgent`` plans over the same logic the deterministic orchestrator runs.
"""
