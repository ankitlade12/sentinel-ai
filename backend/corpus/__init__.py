"""The trusted corpus: per-org, vetted source documents.

The corpus is the org's voice, not the internet's. Sentinel never checks claims
against the web — web search would reintroduce the unreliability it exists to
remove. If the corpus does not cover a topic, that is a quarantine, and the
Trust Report surfaces the gap.
"""
